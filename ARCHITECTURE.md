# Architecture

_Optional placeholder. The lab's architecture is small enough that `BUILD_PLAN.md`, `DOMAIN_MODEL.md`, and `src/` itself usually suffice. Populate this file when an agent or reviewer would otherwise have to read several files in `src/` to answer "where does data flow" or "what are the boundaries."_

_For a real client engagement, fill in:_

- _**Stack** — runtime, framework, test runner, datastore_
- _**Data flow** — request → handler → store → response, in three lines_
- _**Boundaries** — what is in-process, what is external, what is read-only_
- _**Persistence** — where state lives and what is the source of truth_
- _**Testing strategy** — what is unit-tested vs. integration-tested vs. uncovered_

_The reviewer Skill (Lab S2.1) reads this file when it is present. Skip it if your repo is small._
