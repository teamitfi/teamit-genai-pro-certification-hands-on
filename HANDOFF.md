# Handoff — Slice 1: Accounts & Contacts

Slice 1 is complete. `GET /accounts` and `GET /accounts/:id` are implemented in `src/routes/accounts.ts`, loading synthetic seed data from `seeds/accounts.json` at startup; both endpoints return only the fields declared in `DOMAIN_MODEL.md` (`id`, `name`, `industry`, `renewalDate`, `contacts[]`) and actively exclude forbidden fields (`Opportunity.value`, `invoiceAmount`). The slice spec lives in `TASK.md` and `DONE_CRITERIA.md` at the repo root — read those before touching any Slice 2 code. Six tests in `tests/accounts.spec.ts` cover the full contract, with these exact `it()` descriptions: "returns the seeded accounts", "includes renewalDate on each account", "returns [] (not null) for contacts when the account has no contacts", "does not include Opportunity.value or invoiceAmount on any account", "returns the matching account for a known seed id", "returns 404 for an unknown id" — all six pass with `npm test`, `npm run lint`, and `npm run typecheck` exiting 0. A developer sub-agent handled implementation in three iterations (fix `GET /accounts` returning `[]` → add `GET /accounts/:id` with 404 handling and five missing tests → strengthen the `:id` test to assert all declared fields and forbidden-field absence), returning a plain-text summary of changed files and command output after each round. A reviewer sub-agent independently read `PRODUCT_BRIEF.md`, `DOMAIN_MODEL.md`, `TASK.md`, and `DONE_CRITERIA.md` and returned a structured accept/revise/reject verdict with a pass/fail line per checklist item; it issued two revise verdicts (missing `:id` route, thin `:id` test assertions) before a clean accept on the third pass. Nothing is open from this slice. The next recommended slice is **Slice 2: Opportunity Pipeline** — add `seeds/opportunities.json`, wire `GET /opportunities` returning records grouped by stage, and add `tests/opportunities.spec.ts`; decide upfront whether `GET /opportunities/:id` is in scope (it was the main ambiguity in Slice 1 that caused a reviewer revision cycle) and record that decision in `TASK.md` before writing any code. `Opportunity.stage` is case-sensitive (`Prospect`, `Qualified`, `Proposal`, `Closed Won`, `Closed Lost`) and `Opportunity.value` must never appear in any response.
