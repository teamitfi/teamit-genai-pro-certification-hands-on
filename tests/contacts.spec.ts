import { describe, it, expect } from "vitest";

describe("GET /contacts", () => {
  it.skip("returns the contacts for an account", () => {
    expect(true).toBe(true);
  });

  it.skip("returns an empty array when the account has no contacts", () => {
    expect(true).toBe(true);
  });

  it.skip("returns 404 when the account does not exist", () => {
    expect(true).toBe(true);
  });
});
