# Workflow

_Optional: populate during Session 1 if time allows. Document the per-slice loop the team agrees to follow — e.g., plan with `crm-slice-planner` → implement → run tests → review with `crm-reviewer` → handoff note._
