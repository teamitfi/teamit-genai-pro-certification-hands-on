#!/usr/bin/env bash
# Sample git pre-commit hook for Lab S2.3.
# Prefer the tracked hook path:
#   git config --local core.hooksPath scripts/git-hooks
#
# Or copy this file into .git/hooks/pre-commit and make it executable.

exec "$(git rev-parse --show-toplevel)/scripts/lab-s2-3-wrapper.sh"
